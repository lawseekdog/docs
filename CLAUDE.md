# Claude 协作上下文

> 本仓库为文档站（docs：GitHub Pages + just-the-docs）。
>
> 若本文件与 `AGENTS.md` 不一致，以 `AGENTS.md` 为准。

## 约束

- 文档以代码为准：标注“已实现/部分实现/规划中”，避免标准漂移。
- Mermaid 图要兼容 GitHub 渲染器（必要时提供 ASCII 兜底）。
- Pages 默认只在打 tag 时发布，避免 Actions artifacts 配额占用。
